import 'package:flutter/material.dart';

class PersonSectionPage extends StatelessWidget {
  const PersonSectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
