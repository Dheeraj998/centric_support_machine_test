import 'package:flutter/material.dart';

class StorySectionPage extends StatelessWidget {
  const StorySectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
